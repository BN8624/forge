# Forge 결정 기록

## 2026-06-19 프로젝트 분리

- 기존 `atelier`는 참고용으로 보존하고 수정하지 않는다.
- 새 프로젝트는 `C:\Users\USER\forge`에서 독립 실행한다.
- Golem 저장소와 별개의 Git 저장소 및 GitHub 공개 저장소를 사용한다.
- 다른 프로젝트 파일을 런타임에 직접 참조하지 않는다.
- API 설정과 범용 호출 유틸은 Forge 내부로 복사해 자족성을 확보한다.
- 기존 10권 설계와 원고는 새 정본으로 승계하지 않고 `reference/legacy`에 격리한다.
- 신규 작품 목표는 5권이다.

## 2026-06-19 정본 모델

- 구조화 문서가 이야기의 유일한 정본이다.
- 계층은 시리즈, 권, 큰 사건, 장면으로 구성한다.
- 산문은 구조 계약을 입력으로 받아 마지막에 렌더링한다.
- 설정 변경, 복선, 회수에는 정확히 하나의 소유자가 필요하다.
- 장면 후보는 독립 검증 전까지 정본과 상태 원장을 변경할 수 없다.
- 오류는 산문을 수동 수정하지 않고 하네스를 강화한 뒤 올바른 범위를 재생성한다.

## 첫 구현 범위

- 모델 호출 없이 동작하는 JSON 스키마와 결정적 구조 검증기를 먼저 만든다.
- 최소 정상 사례와 소유권 중복 실패 사례로 검증 동작을 증명한다.

## 2026-06-19 이식 결과

- `.env`, `lib/config.py`, `lib/llm.py`, `lib/key_usage.py`, `lib/jsonutil.py`를 Forge 내부로 복사했다.
- 기존 정본 원장, 10권 시리즈 설계, 압축 원고는 `reference/legacy`에 복사했다.
- 10권 설계의 권 배치는 새 정본으로 승계하지 않는다.
- `canon_bible.json`의 C1-C21과 `compressed_manuscript.md`의 기존 세계관·사건·결말은 새 5권 구조의 필수 생성 입력이다.
- 기존 Gemma 모델 ID의 암묵적 폴백을 제거했다.
- `GENERATOR_MODEL`과 `CRITIC_MODEL`이 명시되지 않으면 API 호출 준비 단계에서 실패한다.
- 첫 구조 검증기는 5권 고정, 계층 참조, 순번, 상태 연속성, 이전 장면 연결, 요소 단일 소유권, 복선 선행을 검사한다.

## 검증 기록

- `python -m unittest discover -s tests -v` 통과. 테스트 2개.
- `python -m compileall -q lib pipeline tests` 통과.
- PowerShell `ConvertFrom-Json`으로 스키마 JSON 5개 파싱 통과.
- 참고자료와 결정 기록을 제외한 Forge 파일에서 외부 프로젝트 런타임 경로가 없음을 확인했다.

## 2026-06-19 완전 독립 이전

- Golem 내부에 만들었던 Forge 사본을 사용자 폴더 바로 아래로 복제했다.
- 새 경로는 `C:\Users\USER\forge`다.
- GitHub 저장소는 사용자 요청에 따라 공개 저장소로 생성한다.
- `.env`는 로컬에만 유지하고 Git 추적과 GitHub 업로드에서 제외한다.
- 공개 저장소 `https://github.com/BN8624/forge`를 생성하고 `main` 브랜치를 연결했다.

## 2026-06-19 JSON Schema 검증 연결

- 현재 구조 검증기는 필수 필드 일부와 의미 규칙을 직접 검사하지만 `schemas/*.schema.json`을 실행하지 않는다.
- 속성 타입, 문자열 최소 길이, 허용되지 않은 추가 필드 같은 계약 위반을 놓치지 않도록 Draft 2020-12 검증을 구조 검증의 첫 단계에 연결한다.
- 축약 스키마 구현을 별도로 만들지 않고 표준 `jsonschema` 패키지를 명시적 의존성으로 사용한다.
- 문서가 스키마 검증에 실패하면 해당 문서의 의미 검증은 건너뛰어 잘못된 타입으로 인한 검증기 예외를 막는다.
- `series`, `volume`, `event`, `scene`, `state ledger` 문서 모두 각 Draft 2020-12 스키마를 먼저 통과해야 한다.
- 추가 속성과 잘못된 필드 타입을 거부하는 회귀 테스트를 추가했다.
- `python -m unittest discover -s tests -v` 통과. 테스트 4개.
- `python -m compileall -q lib pipeline tests` 통과.
- 전체 스키마 JSON 파싱과 `Draft202012Validator.check_schema` 검사를 통과했다.

## 2026-06-20 후보 정본 승격

- 이번 범위는 모델 호출로 후보를 만드는 단계가 아니라 이미 준비된 후보 `story` 디렉터리를 검증하고 승격하는 결정적 파이프라인이다.
- 후보는 현재 정본에 덮어쓴 뒤 검사하지 않는다. 프로젝트 내부 임시 디렉터리에 복사한 독립 스냅샷을 `validate_project`로 먼저 검사한다.
- 후보 검증 실패 시 정본은 전혀 변경하지 않는다.
- 정본 교체 중 실패하면 기존 `story` 디렉터리를 백업 위치에서 복구한다.
- 프로세스가 디렉터리 교체 사이에서 중단된 경우 다음 승격 실행이 `.promotion-*` 백업을 탐지해 기존 정본을 복구한다.
- 상태 원장은 이번 승격 대상에 포함하지 않는다. 승격된 구조에서 상태 원장을 재구성하고 재실행 결과를 비교하는 작업은 다음 체크리스트 항목에서 구현한다.
- `python -m unittest discover -s tests -v` 통과. 테스트 8개.
- `python -m compileall -q lib pipeline tests` 통과.

## 2026-06-20 상태 원장 재구성

- 상태 원장은 독립 데이터 입력이 아니라 검증된 구조 문서에서 파생되는 산출물이다.
- `last_scene_id`와 `state`는 마지막 장면의 `id`와 `end_state`를 사용한다.
- `applied_element_ids`는 장면 순서로 수집한다. 같은 장면 안에서는 `changes`, `setups`, `payoffs` 순서를 고정한다.
- 기존 상태 원장은 구조 검증의 입력으로 사용하지 않고, 구조 자체가 유효한지 먼저 검사한 뒤 새 원장을 계산한다.
- JSON 직렬화 형식을 고정하고 임시 파일을 같은 디렉터리에서 `os.replace`해 저장한다.
- 구조 검증 실패 또는 파일 교체 실패 시 기존 `state/current.json`을 보존한다.
- 구조 검증기는 기존 원장의 `last_scene_id`, `state`, `applied_element_ids`를 모두 파생값과 비교한다.
- 변화, 복선, 회수가 섞인 장면 순서와 장면 내 종류 순서를 테스트했다.
- `python -m unittest discover -s tests -v` 통과. 테스트 13개.
- `python -m compileall -q lib pipeline tests` 통과.
- `python pipeline\rebuild_state.py --help` 직접 실행 통과.

## 2026-06-20 구조 후보 생성기

- 기존 `reference/legacy/series_bible_10v.json`은 확장 아이디어 참고자료이며 10권 권 배치는 새 5권 후보에 자동 승계하지 않는다.
- 생성 입력은 `canon_bible.json`, `compressed_manuscript.md`와 선택적 사용자 지시를 결합한다.
- 모델은 `series`, `volumes`, `events`, `scenes`를 담은 단일 JSON 객체를 반환한다.
- 생성기는 응답을 정본 경로가 아닌 임시 후보 디렉터리에 분해하고 기존 `validate_project`로 전체 구조를 검사한다.
- JSON 파싱 실패, 중복 문서 ID, 안전하지 않은 ID, 구조 검증 실패 시 기존 후보 출력은 변경하지 않는다.
- 검증된 임시 후보만 지정 출력 경로로 교체한다. 생성과 정본 승격은 별도 명령으로 유지한다.
- LLM 클라이언트는 함수 인자로 주입할 수 있게 해 실제 API 호출 없이 성공과 실패를 재현한다.
- 실제 CLI는 `brief_file`을 읽고 기본적으로 `runs/candidate`에 후보를 게시한다.
- `google-genai`를 명시적 런타임 의존성으로 추가했다.
- 정상 생성, JSON 파싱 실패, 구조 검증 실패, 중복 ID, 게시 실패 복구를 테스트했다.
- `python -m unittest discover -s tests -v` 통과. 테스트 18개.
- `python -m compileall -q lib pipeline tests` 통과.
- `python -m pip check` 통과.
- `python pipeline\generate_candidate.py --help` 직접 실행 통과.
- 최초 구현 시점에는 `GENERATOR_MODEL`이 없어 실제 모델 호출을 실행하지 않았다.

## 2026-06-20 기존 세계관 입력 정정

- “기존 10권 설계를 승계하지 않는다”를 “기존 세계관을 사용하지 않는다”로 잘못 해석한 결정을 폐기한다.
- 새 작품은 카엘, 리아, 발타자르, 에테르노, 영혼의 조각 5개와 C1-C21의 확정 사건을 유지한다.
- `compressed_manuscript.md`는 기존 사건의 장면 맥락과 인물 표현을 제공하는 필수 참고 입력이다.
- `series_bible_10v.json`의 인물 성장, 세력, 장기 복선 아이디어는 참고할 수 있지만 10권 구조와 추가 설정은 C1-C21보다 우선할 수 없다.
- 5권별 사건 배치나 작품 브리프를 사람이 먼저 작성하지 않는다. Forge 생성 모델이 정본 자료를 읽고 구조 후보를 제안한다.
- 생성 명령은 별도 브리프 없이 실행할 수 있고, 추가 지시는 선택 사항이다.
- `python -m unittest discover -s tests -v` 통과. 테스트 19개.
- `python -m compileall -q -f lib pipeline tests` 통과.

## 2026-06-20 첫 실제 후보 생성 실패

- `gemma-4-31b-it`로 Forge 후보 생성을 실행했으나 구조 검증에서 탈락했다.
- 주요 오류는 권·사건·장면 상태 연속성 단절과 `series.elements`에 정의되지 않은 change 소유였다.
- 실패 후보는 `runs/candidate`에 게시되지 않았다.
- 생성 결과를 사람이 직접 수정하지 않고, 검증 오류와 이전 응답을 모델에 전달해 완전한 번들을 다시 생성하는 제한 재시도 루프를 추가한다.

## 2026-06-20 자동 재생성 결과

- 검증 오류 피드백으로 3회 재생성한 뒤 이야기 요소 소유권 오류는 모두 해소되었다.
- 남은 오류는 전부 권·사건·장면의 `start_state`와 상위 `end_state` 복제 불일치였다.
- 상태 연속성 필드는 창작 판단이 아니라 장면 순서에서 파생되는 구조 필드다.
- 각 장면의 `end_state`는 모델 출력을 유지하고, 다음 장면의 `start_state`, 사건 시작·종료 상태, 권 시작·종료 상태를 Forge가 결정적으로 정규화한다.

## 2026-06-20 두 번째 자동 생성 결과

- 상태 연속성 정규화 뒤 남은 오류는 복선 `S_UNBALANCE`가 소비와 회수보다 뒤에 소유된 1건뿐이었다.
- setup 소유 위치가 소비·회수 이후인 경우 Forge가 최초 소비 또는 회수 장면의 직전 장면으로 소유권을 이동한다.
- 첫 장면에서 바로 소비되는 등 선행 장면이 없어 결정적으로 수정할 수 없는 구조는 계속 검증 실패로 남긴다.

## 2026-06-20 기존 세계관 5권 후보 생성

- Forge 생성기가 기존 정본 자료를 읽어 `runs/candidate`에 구조 후보를 게시했다.
- 생성 결과는 5권, 12개 사건, 24개 장면, 이야기 요소 6개다.
- `python pipeline\validate_structure.py runs\candidate` 통과.
- 제목은 `에테르노의 그림자`이며 카엘, 리아, 발타자르, 에테르노, 다섯 영혼의 조각, 피의 제사, 제국의 눈 파괴 결말이 구조 문서에 반영되었다.
- 구조 검증 통과와 핵심 문자열 존재만으로 C1-C21 전체의 의미 준수를 증명할 수 없으므로 정본 승격은 보류한다.
- 다음 단계는 각 C1-C21을 소유 장면에 추적하고 누락·충돌을 독립 검증하는 계약 추가다.
- `python -m unittest discover -s tests -v` 통과. 테스트 23개.
- `python -m compileall -q -f lib pipeline tests` 통과.
- `python -m pip check` 통과.

## 2026-06-20 정본 의미 독립 검증

- 의미 판정은 사람이 직접 수행하지 않고 Forge의 `critic` 역할이 수행한다.
- critic은 C1-C21 각각에 `pass`, `fail`, `uncertain`, 근거 장면 ID 목록, 판정 이유를 반환한다.
- 하네스는 21개 ID의 정확한 집합, 장면 ID 실재 여부, 전체 `pass`, 후보 `story` SHA-256 일치를 결정적으로 검사한다.
- 검토 결과는 후보 루트의 `canon-review.json`에 저장한다.
- 정본 승격기는 승인된 최신 검토 파일이 없는 후보를 거부한다.
- 생성 모델과 critic 모델은 역할을 분리하지만 현재 로컬 설정에서는 모두 사용자가 지정한 Gemma 4 31B를 사용한다.

## 2026-06-20 정본 의미 검증 및 승격 결과

- Forge critic이 C1-C21을 각각 한 번씩 판정했고 21개 모두 `pass`였다.
- 각 판정은 근거 장면 ID와 이유를 포함하며 후보 구조 해시와 일치한다.
- 승인 파일은 정본 구조와 함께 `story/canon-review.json`에 원자적으로 승격된다.
- 승인 뒤 후보를 정본 `story`로 승격하고 `state/current.json`을 재구성했다.
- 최종 장면은 `V5-E04-S02`, 최종 상태는 `새로운 여명`이다.
- `python -m unittest discover -s tests -v` 통과. 테스트 31개.
- `python -m compileall -q -f lib pipeline tests` 통과.
- `python -m pip check` 통과.
- `python pipeline\validate_canon.py runs\candidate` 통과.
- `python pipeline\validate_structure.py` 통과.

## 2026-06-20 장면별 산문 생성

- 산문은 Forge generator가 정본 장면 계약에서 생성하고 Forge critic이 독립 검증한다.
- 사람은 생성 문장을 직접 수정하지 않고 프롬프트, 검증 계약, 재시도 규칙만 조정한다.
- 장면은 정본 순서대로만 생성한다. 첫 장면이 아니면 `previous_scene_id`의 승인된 정본 산문이 반드시 있어야 한다.
- 생성 입력은 시리즈·권·사건·장면 계약, 장면에 연결된 C1-C21 근거, 기존 압축 원고, 이전 정본 산문이다.
- critic은 목표 달성, 시작·종료 상태 전이, 소유 요소 반영, 정본 충돌, 이전 산문 연속성, 산문 자체 완결성을 판정한다.
- 승인 산문과 검토 결과는 `prose/scenes/<scene-id>/prose.md`, `review.json` 한 디렉터리로 묶어 원자적으로 승격한다.
- 실패하면 해당 장면 후보만 폐기하고 critic 지적을 generator에 전달해 제한 횟수 재생성한다.

## 2026-06-20 산문 하네스 실증과 규모 차단

- Forge가 첫 3개 장면을 실제 생성했고 각 장면은 critic의 6개 검사와 해시 검증을 통과했다.
- 배치 모드는 승인된 이전 장면을 입력으로 다음 장면을 순차 생성하는 경로까지 확인했다.
- 규모 감사에서 현재 권별 목표 분량은 V1 4,000자, V2 4,000자, V3 2,500자, V4 2,700자, V5 7,200자로 확인되었다.
- 이는 5권 장편 규모에 미달하므로 실증 산문 3개는 정본 커밋하지 않고 제거했다.
- 기존 10권 설계의 권당 약 10만 자 목표를 근거로 권별 최소 8만 자 규모 게이트를 추가했다.
- 산문 생성기는 규모 게이트를 통과하기 전에는 모델 호출을 시작하지 않는다.
- 다음 단계는 사람이 장면을 직접 추가하는 것이 아니라 Forge가 현재 정본의 사건·장면 계층을 장편 규모로 확장하는 것이다.
- `python -m unittest discover -s tests -v` 통과. 테스트 40개.
- `python -m compileall -q -f lib pipeline tests` 통과.
- `python -m pip check` 통과.
- `python pipeline\validate_scale.py`는 현재 5개 권 모두 규모 미달로 의도된 실패를 반환한다.

## 2026-06-20 장편 구조 확장

- 5권 전체의 확장 구조를 한 모델 응답에 요구하지 않고 권별로 생성해 출력 한도와 재시도 범위를 제한한다.
- 각 권은 최소 20개 장면, 장면 목표 글자 수 합계 최소 8만 자를 충족해야 한다.
- 현재 정본 권의 목표, 시작·종료 역할, 기존 사건의 핵심 의미를 확장의 앵커로 사용한다.
- 기존 `series.elements`와 권별 소유 요소 ID 집합은 변경하지 않는다. Forge는 같은 권 안에서 소유 장면만 재배치할 수 있다.
- 권별 결과를 모두 조립한 뒤 previous_scene_id, 상태 연속성, 복선 순서를 결정적으로 정규화하고 전체 구조·규모 검증을 수행한다.
- 확장 후보는 별도 경로에 게시하며 critic C1-C21 검증 전에는 정본으로 승격하지 않는다.

## 2026-06-20 첫 장편 확장 실행

- 실제 권별 확장에서 V1은 검증을 통과했고 V2 응답이 3회 모두 `volume/events/scenes` 계약으로 파싱되지 않아 중단되었다.
- 실패 원문을 보존하지 않아 정확한 응답 형태를 확인할 수 없었던 관측 공백을 보완한다.
- 정본 구조 SHA-256별 작업 디렉터리에 검증된 권 응답을 캐시하고 실패 응답 원문을 기록한다.
- 재실행은 이미 검증된 권 호출을 반복하지 않고 실패한 권부터 이어간다.

## 2026-06-20 장편 확장 및 첫 산문 실행

- Forge가 5권 126개 장면, 총 목표 456,300자의 확장 후보를 생성했다.
- 권별 목표는 V1 93,700자, V2 84,500자, V3 91,500자, V4 96,000자, V5 90,600자다.
- 구조·규모 검증과 critic C1-C21 검증을 통과해 확장 구조를 정본으로 승격했다.
- 첫 산문 생성은 generator 응답 형식 오류로 3회 실패했으며 산문 정본은 생성되지 않았다.
- 산문 generator·critic 실패 원문을 장면 계약 해시별 작업 디렉터리에 기록해 실제 출력 형태를 근거로 계약을 조정한다.

## 2026-06-20 장편 구조 승격과 산문 시작

- Forge가 권별 확장 구조를 생성해 5권 126개 장면, 총 목표 456,300자를 구성했다.
- 권별 목표는 V1 93,700자, V2 84,500자, V3 91,500자, V4 96,000자, V5 90,600자다.
- 확장 후보는 구조·규모 검증과 critic C1-C21 전체 `pass`를 통과해 정본으로 승격됐다.
- 권별 생성 응답과 실패 원문은 정본 구조 해시별 작업 캐시에 보존되어 중단 후 재개할 수 있다.
- 산문 critic에 `reveal_order` 검사를 추가해 미래 C1-C21 항목과 비소유 요소의 선취를 차단한다.
- 첫 장면 `V1-E01-S01` 산문은 2,224자이며 7개 검사 전체와 해시 검증을 통과했다.
- 첫 장면은 미래 금지 항목인 C1, C10, C11 등을 노출하지 않고 현재 장면의 C8, C9만 반영한 것으로 critic이 승인했다.
- `python -m unittest discover -s tests -v` 통과. 테스트 49개.
- `python -m compileall -q -f lib pipeline tests` 통과.
- `python -m pip check` 통과.
- `python pipeline\validate_structure.py` 통과.
- `python pipeline\validate_scale.py` 통과.

## 2026-06-20 산문 배치 생성 진행

- Forge가 정본 순서대로 19개 장면 산문을 생성했고 모두 critic 승인을 통과했다.
- 현재 승인 산문 합계는 48,610자이며 각 산문과 review 해시가 일치한다.
- `V1-E02-S02`에서 C1의 허용된 팔 부재 사실과 미래 EL-03의 불균형 기능을 critic이 혼동한 사례가 있었다.
- critic 계약을 보강해 이미 공개된 물리적 사실의 언급과 미래 요소의 기능·인과·기술 설치를 구분한다.
- 이후 문제 장면과 다음 10개 장면이 새 계약으로 승인됐다.
- 다음 배치는 자동으로 `V1-E03-S05`부터 시작한다.

## 2026-06-20 V1 산문 완료

- Forge가 V1의 29개 장면 산문을 모두 생성했고 critic 승인을 통과했다.
- V1 실제 산문 합계는 75,959자이며 모든 review 상태, 7개 checks, 산문 해시가 유효하다.
- 짧은 산문 후보를 전체 재작성하는 대신 부족 분량만 `addition`으로 이어 쓰는 복구 경로를 추가했다.
- critic 의미 실패 시 잘못된 후보를 이어 쓰지 않고 폐기한 뒤 전체 장면을 재작성한다.
- 현재 장면 objective를 의미 공개의 상한으로 사용하고 다음 두 장면의 예약 목표를 generator와 critic에 전달한다.
- 미래 금지 항목은 정확한 문구뿐 아니라 동의어·은유·추측·예감으로 암시해도 실패 처리한다.
- 다음 산문 생성 시작점은 `V2-E01-S01`이다.

## 2026-06-20 V1 모바일 뷰어

- 산문 정본은 수정하지 않고 `story`의 권·사건·장면 순서와 승인된 `prose.md`만 읽는 별도 뷰어를 둔다.
- 외부 공개용 Funnel은 사용하지 않고 같은 tailnet 기기만 접근할 수 있게 한다.
- 기존 Tailscale Serve가 8002 포트를 사용하고 있으므로 Forge 뷰어는 충돌하지 않는 8765 포트를 사용한다.
- 별도 앱 의존성을 추가하지 않고 Python 표준 라이브러리 HTTP 서버로 구현한다.
- 추가 Tailscale Serve 엔드포인트 설정은 응답 없이 대기해 중단했다. 기존 8002 서비스는 변경하지 않았다.
- 대신 서버를 이 장치의 Tailscale IP `100.89.73.83`에만 바인딩하고 8765 포트로 제공한다. tailnet 밖의 일반 네트워크 인터페이스에는 열리지 않는다.
- iPhone 폭 390px에서 가로 넘침 없이 4개 사건과 29개 장면이 렌더링되는 것을 브라우저로 확인했다.

## 2026-06-20 V1 EPUB

- Safari 연속 스크롤 외에 iPhone 도서 앱에서 오프라인으로 읽을 수 있도록 EPUB을 파생한다.
- 외부 변환 도구나 라이브러리를 추가하지 않고 Python 표준 라이브러리로 EPUB 3 컨테이너를 생성한다.
- 사건별 본문 파일과 EPUB 목차를 만들되, 독자에게 내부 장면 계약이나 사건 objective는 노출하지 않는다.
- 생성 파일은 파생 산출물이므로 `exports`에 두고 Git 정본에는 포함하지 않는다.
- V1 EPUB은 4개 사건 본문에 29개 장면을 정본 순서대로 담으며 현재 크기는 68,655바이트다.
- EPUB의 mimetype 무압축 선두 배치, 컨테이너, 패키지, 목차, XHTML XML 구문을 검증했다.
- Tailscale 뷰어의 `/V1.epub` 응답은 `application/epub+zip`과 첨부 파일 헤더를 제공한다.

## 2026-06-20 V2 산문 생성 시작

- 본 작업의 다음 정본 장면은 `V2-E01-S01`이며 V2에는 아직 승인된 산문이 없다.
- Forge의 기존 generator·critic 하네스를 변경하지 않고 `--all --limit 10`으로 첫 배치를 실행한다.
- 산문 내용은 직접 수정하지 않으며 실패 시 하네스가 해당 장면만 피드백 기반으로 재생성한다.
- `V2-E01-S04`가 다음 장면의 외곽 진입과 감정을 선취해 두 실행 연속 critic에서 거부됐다.
- generator와 짧은 후보 확장 프롬프트에 `end_state` 도달 순간을 절대 종료선으로 명시하고, 이후 장소·행동·감정 묘사를 금지했다.
- 강화된 종료 경계로 `V2-E01-S04`를 재생성한 뒤 `V2-E02-S05`까지 10개 장면을 순차 승인했다.
- V2 승인 산문 합계는 30,373자이며 모든 review 상태, 7개 checks, 산문 SHA-256이 유효하다.
- 다음 산문 생성 시작점은 `V2-E03-S01`이며 전체 남은 장면은 87개다.

## 2026-06-20 V2 두 번째 산문 배치

- `V2-E03-S01`부터 최대 10개 장면을 기존 generator·critic 하네스로 생성한다.
- 이 배치는 V2-E03 전체와 V2-E04 앞부분을 포함하며 강화된 `end_state` 종료 경계를 그대로 적용한다.
- 산문은 직접 수정하지 않고, 승인 실패 원문과 critic 판정을 근거로 하네스 문제만 조정한다.
- `V2-E04-S05`의 objective에 있는 “변칙적인 움직임”을 generator가 미래 EL-04의 불균형 검술·마력 방패 돌파로 세 번 해석해 critic이 거부했다.
- objective 어휘가 미래 금지 요소와 겹쳐도 사용 권한이 생기지 않으며, 동료·환경·타이밍·상대 실수 등 다른 원인으로 목표를 달성하도록 generator와 확장 프롬프트를 보강했다.
- 보강된 프롬프트로 `V2-E04-S05`가 미래 EL-04를 사용하지 않고 승인됐다.
- 두 번째 배치 10개 장면은 32,153자이며 V2 누적은 20개 장면, 62,526자다.
- 모든 V2 review 상태, 7개 checks, 산문 SHA-256이 유효하며 다음 시작점은 `V2-E04-S06`이다.
- 전체 남은 장면은 77개다.

## 2026-06-20 5권 산문 완주

- 사용자 승인에 따라 남은 77개 장면은 중간 확인 없이 Forge가 끝까지 생성·critic 검증·승격한다.
- 반복 실패 시 산문을 직접 고치지 않고 실패 원문과 critic 근거로 일반 하네스 계약만 보강한다.
- 권 단위로 승인 해시와 회귀 검증을 수행하고 의미 단위 커밋을 푸시한다.
- 완료 조건은 126개 전 장면 승인, 7개 critic checks와 해시 유효, 전체 테스트·구조·규모 검증 통과, 5권 EPUB 생성과 Tailscale 제공이다.
- V2 남은 3개 장면을 승인해 V2 전체 23개 장면, 72,379자를 완성했다.
- V2 모든 critic checks와 산문 해시가 유효하며 다음 시작점은 `V3-E01-S01`이다.
- `V3-E04-S06`은 objective 자체가 미래 EL-04와 일부 겹쳐 generator가 이를 재사용 가능한 검술 각성으로 일반화했다.
- objective와 미래 요소가 부분 중첩될 때 현재 행동은 가장 좁은 일회성 우발 사건으로만 허용하고 기술·성장·각성으로 일반화하지 않도록 프롬프트를 보강했다.
- 일반 규칙만으로도 EL-04 일반화가 반복되어, EL-04가 미래 금지인 동안 신체 불균형을 승리 원인이나 검술 원리로 쓰지 않고 리아·환경·상대 실수를 결정 원인으로 두는 충돌 경계를 자동 주입한다.
- `V4-E04-S05`처럼 objective가 방패 사각지대 공격을 직접 요구할 때는 외부 원인이 먼저 방패를 무력화하고 카엘은 기존 정석 공격만 사용하도록 EL-04 방패 장면 필수 해결법을 추가했다.
- 보강된 방패 장면 경계로 `V4-E04-S05`를 승인하고 V4 전체 24개 장면, 85,238자를 완성했다.
- V4 모든 critic checks와 산문 해시가 유효하며 다음 시작점은 `V5-E01-S01`이다.
- V5 전체 26개 장면, 80,385자를 승인해 5권 산문을 완성했다.
- 최종 산문은 126개 장면, 391,363자이며 권별 분량은 V1 75,959자, V2 72,379자, V3 77,402자, V4 85,238자, V5 80,385자다.
- 126개 review의 상태, 7개 checks, 산문 SHA-256을 전부 검증했다.
- 전권 EPUB 5개를 생성하고 각 컨테이너·패키지·목차·XHTML XML 구문을 검증했다.
- Tailscale 뷰어를 전권 서재로 확장해 5권 읽기 링크와 EPUB 다운로드를 제공하며 iPhone 390px 폭에서 가로 넘침이 없음을 확인했다.

## 2026-06-21 전 과정 자동화

- 기본 자동화 명령은 현재 유효한 정본 구조와 승인 산문을 재사용하며 미완료 단계만 실행한다.
- 정본 구조가 없거나 유효하지 않으면 5권 후보 생성, critic C1-C21 검증, 승격, 상태 원장 재구성을 순서대로 실행한다.
- 구조가 장편 규모 미달이면 권별 확장 후보 생성, critic 재검증, 승격, 상태 원장 재구성을 자동 실행한다.
- 구조 강제 재생성은 명시적 옵션으로만 허용하며 구조가 바뀔 때 기존 산문은 `runs/prose-backups`로 이동하고 승격 실패 시 복구한다.
- 산문 생성은 장면 단위 승인 상태에서 재개하고 실패 장면은 설정된 횟수만큼 전체 생성 실행을 재시도한다.
- 최종 단계에서 126개 산문 review·해시, 구조, 규모를 검증하고 V1-V5 EPUB을 다시 생성한다.
- `pipeline/complete_series.py`를 단일 진입점으로 추가했다. 기본 실행은 유효한 정본과 승인 산문을 건너뛰며 모델 클라이언트도 실제 생성 호출 전에는 만들지 않는다.
- 진행 상태는 `runs/complete-series/status.json`에 원자적으로 기록한다.
- `STOP_AFTER_RUN`이 이미 있으면 새 장면을 시작하지 않고, 생성 중 만들어지면 현재 승인 장면 뒤에 멈춘다.
- `--scene-retries 0`은 장면 성공까지 계속 재시도하며 기본값은 전체 생성 실행 5회다.
- `--regenerate-structure`는 후보를 새로 생성하고 구조가 달라질 때 기존 산문을 `runs/prose-backups`에 보존한다. 승격 실패 시 산문 디렉터리를 복구한다.
- 기존 산문 중 최초 미승인·해시 불일치·계약 불일치 장면을 찾으면 그 장면부터 뒤쪽 산문만 작업 백업하고 앞의 유효한 승인 산문은 재사용한다.
- 완성된 현재 저장소에서 `python pipeline\complete_series.py`를 실행해 모델 호출 없이 126개 장면 검증과 EPUB 5개 재생성을 완료했다.
- `python -m unittest discover -s tests -v` 통과. 테스트 63개.
- `python -m compileall -q -f lib pipeline tests` 통과.
- `python -m pip check` 통과.
- `python pipeline\validate_structure.py`와 `python pipeline\validate_scale.py` 통과.

## 2026-06-21 신규 세계관 자동 완주

- `--new-world`는 기존 작품의 고유 인물·장소·장치를 금지하고 제목, 장르, 톤, 전제, C1-C21 정본, 3천 자 이상 압축 참고 원고를 먼저 생성한다.
- 생성 세계관은 `reference/current`에 원자적으로 게시되며 모든 구조·critic·산문 프롬프트가 legacy보다 이를 우선 사용한다.
- 신규 세계관의 series.title과 series.premise는 원천 세계관 값과 정확히 일치해야 구조 후보가 통과한다.
- 첫 신규 실행 전에 현재 story, prose, state, exports, reference/current를 `runs/world-backups`에 복사한다.
- `runs/new-world/active.json`이 active이면 같은 `--new-world` 명령은 새 세계를 만들지 않고 진행 중 세계에서 재개한다. complete 뒤 다시 실행하면 새 세계를 시작한다.
- 당시 신규 세계관 모드는 장면 전체 생성 실행 재시도 기본값을 무제한으로 사용했다. 이후 기본 5회로 변경됐다.
- 생성 세계관의 21개 정본 ID는 기존 review 스키마와 추적성을 유지하기 위해 C1-C21 형식을 쓰지만 내용은 모두 새로 생성한다.
- 전체 테스트 70개, 구문 검사, 의존성 검사, 구조·규모 검증을 통과했다.
- 현재 완성본에서 일반 `python pipeline\complete_series.py` 재실행도 모델 호출 없이 통과해 기존 모드 회귀가 없음을 확인했다.
- 첫 실제 신규 실행에서 세계관과 정본은 생성됐지만 압축 참고 원고가 3,000자 미만이라 게이트에서 정지했다.
- 신규 세계관 원고가 1,000자 이상 3,000자 미만이면 전체 세계관을 버리지 않고 Forge가 결말 이후의 여파와 사회 변화를 추가 산문으로 이어 써 최소 계약을 충족하도록 복구 경로를 추가했다.
- 재개된 신규 세계관의 첫 구조 후보는 C6의 부작용, C7의 제조 독점 주체, C14의 과거 실험 장소를 장면 구조에 직접 배치하지 않아 critic이 거부했다.
- 구조 generator가 모든 정본 항목의 조건·주체·부작용·장소·과거 사건을 최소 한 장면의 objective·상태·소유 요소에서 직접 입증 가능하게 배치하고 ID별 근거를 자체 점검하도록 계약을 강화했다.
- 강화된 구조 후보는 정본 검증을 통과했으나 V5 장편 확장에서 canon ID C18·C13을 `consumes_setups`로 잘못 사용해 중단됐다.
- 확장 프롬프트에 허용 setup 요소 목록을 직접 전달하고 canon·change·payoff ID 사용을 금지했다. 응답의 `consumes_setups`는 정의된 setup ID만 남기도록 결정적으로 정규화한다.
- 보강된 EL-04 충돌 경계로 `V3-E04-S06`과 마지막 장면을 승인해 V3 전체 24개 장면, 77,402자를 완성했다.
- V3 모든 critic checks와 산문 해시가 유효하며 다음 시작점은 `V4-E01-S01`이다.

## 2026-06-22 신규 세계관 실제 완주

- Forge가 생성한 신규 작품 제목은 `은색의 기록자와 망각의 태엽`이며 장르는 스팀펑크 다크 판타지다.
- 원천 세계관, C1-C21 정본, 압축 참고 원고를 `reference/current`에 보존한다. 구조화 문서 재현에 필요한 현재 정본이므로 Git 추적 대상에 포함한다.
- 자동화가 5권 20개 사건, 108개 장면의 구조와 산문을 완성했다.
- 승인 산문은 총 375,597자다. 권별 장면 수와 분량은 V1 20개·71,192자, V2 21개·74,982자, V3 26개·83,276자, V4 21개·74,621자, V5 20개·71,526자다.
- 108개 review의 승인 상태, 7개 critic checks, 산문 SHA-256, 장면 계약을 독립 검사해 오류가 없음을 확인했다.
- V1-V5 EPUB을 생성했다. 파일 크기는 각각 62,738, 66,236, 72,292, 63,836, 61,364바이트다.
- Tailscale 전권 서재를 새 작품으로 다시 띄웠다. 주소는 `http://node.tail3e9e21.ts.net:8765/`이다.
- iPhone 390×844 뷰포트에서 제목과 EPUB 링크 5개를 확인했고 문서 폭 390px, 스크롤 폭 390px로 가로 넘침이 없었다.
- 이전 완성본은 실행 시점 백업인 `runs/world-backups/20260621T020758.714624Z`에 보존한다.
- 실제 완주 후 `python -m unittest discover -s tests -v` 72개, `python -m compileall -q -f lib pipeline tests`, `python -m pip check`, 구조·규모 검증이 모두 통과했다.

## 2026-06-22 게임 시나리오 기획 자동 선택

- 사용자가 장르나 소재를 먼저 선택하지 않고 Forge가 게임 시나리오로 확장하기 좋은 소설 기획 후보를 여러 개 제안하도록 한다.
- generator는 핵심 플레이 반복, 플레이어 역할, 성장 구조, 세력, 선택 구조, 5권 서사 확장성이 서로 다른 후보 5개를 만든다.
- 별도 critic은 소설 완결성, 게임 핵심 반복, 플레이어 행위성, 콘텐츠 확장성, 차별성을 기준으로 모든 후보를 평가하고 하나를 선택한다.
- 선택된 후보와 critic의 위험 요소·보완 지시만 신규 세계관 생성의 구속 입력으로 사용한다. 사람이 산문이나 설정을 직접 작성하지 않는다.
- 후보 목록, critic 평가, 최종 선택본은 이후 결정 추적을 위해 생성 세계관 원천과 함께 보존한다.
- 기존 `--new-world`의 즉시 세계관 생성 동작은 유지하고 게임 기획 자동 선택은 별도 진입점으로 제공한다.
- 새 진입점은 `python pipeline\complete_series.py --game-scenario`다.
- 후보는 S1-S5를 정확히 한 번씩 포함하며 제목, 장르, 로그라인, 플레이어 역할, 핵심 반복, 성장, 세력, 선택 구조, 5권 전개, 게임 적합성을 필수로 가진다.
- critic은 후보별 다섯 기준의 1-10점 평가, 강점, 위험, 전체 순위, 선택 근거, 세계관 보완 지시를 반환해야 한다.
- 선택된 후보의 제목과 장르는 세계관 생성 결과와 정확히 일치해야 하며 불일치하면 세계관 전체 응답을 재생성한다.
- 결과는 `reference/current`의 `synopsis-candidates.json`, `synopsis-review.json`, `selected-synopsis.json`으로 세계관 원천과 함께 보존한다.
- `python -m unittest discover -s tests -v` 80개, 전체 구문 검사, `python -m pip check`, 구조·규모 검증이 통과했다.
- 현재 완성본에서 `python pipeline\complete_series.py`를 다시 실행해 모델 호출 없이 108개 장면과 EPUB 5개를 재검증했다.

## 2026-06-22 iPhone 시작 대시보드

- 기존 Tailscale 전권 서재의 `/dashboard`에 모바일 제어 화면을 추가한다.
- 대시보드는 선택 지시 입력, 시놉시스 후보 생성, critic 추천과 후보별 강점·위험 확인, 후보 선택, 5권 자동 완주 시작, 진행 상태 조회를 제공한다.
- 후보 생성과 5권 자동 완주는 HTTP 요청 스레드가 아니라 숨겨진 백그라운드 Python 프로세스로 실행한다.
- 서버는 동시에 하나의 Forge 작업만 허용하며 실행 PID, 작업 종류, 시작·종료 상태를 `runs/dashboard/job.json`에 기록한다.
- POST 요청은 서버 시작 시 생성한 난수 토큰을 같은 출처 페이지가 헤더로 보내야 실행된다. 서버는 Tailscale IP에만 바인딩한다.
- 사용자가 critic 추천과 다른 후보를 고르면 `concept-selection.json`에 사용자 선택과 원래 추천을 함께 보존한다.
- 대시보드가 시작하는 자동화 명령은 `--game-scenario --reuse-concept --selected-synopsis S#`다.
- 전권 서재는 요청마다 현재 구조와 산문을 읽어 새 작품 완주 뒤 서버 재시작 없이 새 정본을 표시한다.
- 중단된 신규 작품이 있으면 새 후보 시작을 차단하고 기존 `--game-scenario` 실행을 재개하는 버튼을 제공한다.
- 실제 서버를 Tailscale IP `100.89.73.83`의 8765 포트에 다시 띄웠으며 최종 검증 시 PID는 42280이다.
- `http://node.tail3e9e21.ts.net:8765/dashboard`가 HTTP 200으로 응답하고 토큰 없는 POST 요청은 HTTP 403으로 거부됨을 확인했다.
- iPhone 390×844 뷰포트에서 문서 폭과 스크롤 폭이 모두 390px라 가로 넘침이 없었다. 후보 생성, 상태 갱신, 선택 시작, 중단 재개 버튼과 현재 작품 상태를 확인했고 브라우저 경고·오류는 없었다.
- 전체 테스트 87개, 구문 검사, 의존성 검사, 구조·규모 검증이 통과했다.
- `python pipeline\complete_series.py` 재실행은 모델 호출 없이 108개 장면과 EPUB 5개를 검증했다.

## 2026-06-22 대시보드 시작 잠금 수정

- 후보 생성 프로세스가 종료된 뒤에도 Windows의 PID 생존 판정이 `running`으로 남아 선택 시작 버튼이 잠기는 문제를 확인했다.
- Windows에서는 `OpenProcess`와 `GetExitCodeProcess`로 실제 프로세스 생존 여부를 판정하도록 변경했다.
- 대시보드 서버가 재시작돼 `Popen` 객체를 잃은 경우에도 후보 결과 파일 시각과 신규 작품 완료 원장의 시각으로 외부 작업 성공을 복구한다.
- 대시보드 요청 토큰은 `runs/dashboard/token.txt`에 보존해 서버 재시작 뒤 열린 페이지의 POST 요청이 불필요하게 무효화되지 않게 했다.
- 새 후보 생성 중에는 이전 후보 카드를 숨기고 생성·평가 중이라는 상태만 표시해 기존 후보를 새 결과로 오인하지 않게 했다.
- 사용자가 선택한 S1 `잿더미 도시의 기억 기록자` 자동 완주 요청은 정상 접수됐으며 백그라운드 프로세스에서 실행 중이다.
- 수정 후 전체 테스트 90개, 구문 검사, 의존성 검사가 통과했다.

## 2026-06-22 대시보드 상세 진행 표시

- 내부 단계명만 표시하던 상태 영역을 대상 작품, 한국어 단계 설명, 7단계 위치, 예상 전체 진행률, 정확한 장면 승인 수량으로 확장했다.
- 현재 장면 ID와 전체 순번, 현재 권, critic 시도 횟수, 실행 시간, 마지막 상태 갱신 시각을 표시한다.
- 장면 전체 순서는 현재 정본의 시리즈·권·사건 문서에서 매 요청마다 계산하고, 산문과 review가 모두 있으며 status가 pass인 장면만 승인 수량으로 센다.
- 생성 중 서재 서버가 미승인 장면을 읽다가 시작 자체에 실패하던 문제를 수정했다. 전권 미완성 중에는 준비 화면과 대시보드 링크를 제공한다.
- 실제 진행 상태는 126개 장면 중 2개 승인, 현재 3번째 장면 `V1-E01-S03`, V1, critic 1회차다.
- iPhone 390×844에서 6개 진행 지표와 진행 막대를 확인했고 가로 넘침과 브라우저 오류가 없었다.
- 전체 테스트 92개, 구문 검사, 의존성 검사가 통과했다.

## 2026-06-22 대화와 진행 속도 품질 문제

- 현재 작품의 초반 산문은 감각·상황·내면 묘사를 반복하고 대화와 인물 선택이 부족하다는 사용자 피드백을 받았다.
- 표본상 V1-E01-S01은 3,747자 전체가 독백과 묘사이며 대화가 없다. 이후 장면도 대화가 설정 설명에 치우치거나 대화 비중이 1-6%에 그치는 경우가 반복됐다.
- critic은 장면 목표와 설정 순서만 확인하고 반복 묘사, 정보 전달용 독백, 장면 내 시도·방해·결정의 부재를 모두 `prose_quality: true`로 승인했다.
- 구조 objective도 `공포 묘사`, `기능 설명`, `규칙 학습`처럼 정적인 표현을 사용해 느린 설명 장면을 유도했다.
- 같은 품질의 누적을 막기 위해 실행 중이던 자동 완주 프로세스를 종료했다. 정본 구조와 26개 승인 산문은 남아 있으며 새 품질 계약 적용 시 작업 백업 후 재생성한다.

## 2026-06-22 대화와 진행 속도 품질 계약 적용

- 구조 생성기와 확장기는 장면 목표에 즉시 목표, 방해, 행동 또는 선택, 관찰 가능한 결과를 요구한다. `공포 묘사`, `기능 설명`, `규칙 학습`, `장소 소개`처럼 정적인 목표는 금지한다.
- 산문 생성기는 시도, 방해, 대응, 결과의 진행 비트와 장면 말미의 결정 또는 결과를 요구한다. 정적인 묘사 문단의 연속, 같은 깨달음의 반복, 정보 전달만 하는 대화를 금지한다.
- 대화가 가능한 장면은 대화 블록 4개 이상과 전체 글자 수 대비 대화 8% 이상을 critic 호출 전에 검사한다. 생성 프롬프트의 목표 대화 비중은 15-35%다.
- critic은 반복 묘사, 정보 전달용 독백, 시도·방해·선택·결과 부재, 상태 변화 없는 결말을 산문 품질 실패로 판정한다.
- 산문 계약 버전을 2로 올려 기존 review를 무효화했다. 이전 26개 산문은 `runs/prose-backups/20260622T083639.659168Z`에 보존했다.
- 새 계약으로 재생성한 표본은 V1-E01-S01 3,011자와 대화 0%, V1-E01-S02 3,348자와 대화 31.8%, V1-E01-S03 3,307자와 대화 18.5%다. 첫 장면은 단독 각성 장면이라 대화 예외를 적용했고 세 번 이상의 구체적 시도와 실패 또는 예상 밖 결과를 요구했다.
- 표본 3개 모두 새 critic 검증을 통과했다. 전체 자동 완주는 3개 승인 상태에서 V1-E01-S04부터 재개했다.
- `python -m unittest discover -s tests -v` 전체 94개, `python -m compileall -q -f lib pipeline tests`, `python -m pip check`, `python pipeline\validate_structure.py`, `python pipeline\validate_scale.py`가 통과했다.

## 2026-06-22 과도한 산문 재시도 진단

- 새 품질 계약 적용 뒤 V1-E02-S03이 18회째 재시도에 진입했다.
- 이는 정상적인 품질 강화 범위를 벗어나며 장면 구조, 생성 지시, 선검사 또는 critic 피드백 루프 사이의 충돌로 취급한다.
- 같은 실패를 무제한 반복시키지 않고 현재 실행을 중단한 뒤 실제 실패 원인을 읽어 하네스와 원인 계약을 수정한다.

## 2026-06-22 정적 구조와 재시도 루프 교체

- V1-E02-S03은 C5 `기억의 닻`을 공개해야 하는 장면에서 미래 금지 C4 `자아 붕괴`를 반복 선취해 critic에 거부됐다. 이후 생성 후보는 목표 길이에 미달했으며 바깥 재시도가 같은 작업 파일과 실패 상태를 덮어쓰며 18회까지 반복했다.
- 산문 작업 디렉터리에 마지막 실패 사유를 보존하고, 길이·스타일·critic 오류를 덮어쓰지 않고 누적해 다음 생성 호출에 전달한다. 생성 실패 산출물도 실행을 넘겨 고유 번호로 보존한다.
- 신규 세계관과 게임 시나리오 모드의 장면 실행 재시도 기본값을 무제한에서 5회로 변경했다. 반복 실패는 숨기지 않고 구조나 계약 결함으로 노출한다.
- 기존 126개 장면 목표 대부분이 `공포 묘사`, `기능 설명`, `규칙 학습`, `시각적 구현` 형태라 산문 프롬프트만으로 해결할 수 없음을 확인했다.
- 같은 세계관과 선택 기획을 유지하면서 구조를 다시 생성했다. 새 구조는 총 110장면이며 권별 목표는 V1 24장면 96,800자, V2 20장면 84,800자, V3 20장면 84,900자, V4 21장면 88,300자, V5 25장면 89,900자다.
- 새 목표는 `장치 수리 후 신호 포착`, `소라를 추궁하고 파트너십 체결`, `파편을 미끼로 적을 유인해 철문 탈출`처럼 행동, 방해, 선택, 결과가 포함된다. 구조와 권별 규모 검증을 통과했다.
- 구조 교체 전 산문은 `runs/prose-backups/20260622T111610.181649Z`에 보존했다.
- 생성 모델 호출에 최대 출력 8,192토큰을 명시하고, 짧은 초안을 다음 재작성 호출에 실제로 전달하도록 수정했다. 단독 행동 장면은 대화 장면보다 자연스럽게 짧은 점을 반영해 최소 길이 비율을 65%로 분리하고 대화 장면은 70%를 유지했다.
- 새 표본 3장면이 모두 critic 승인을 받았다. V1-E01-S01은 2,678자와 구체적 행동 시도 3회 이상, V1-E01-S02는 3,767자·대화 12회·19.9%, V1-E01-S03은 2,631자·대화 6회·14.4%다.
- 전체 97개 테스트, 구문 검사, 의존성 검사, 구조 검증, 권별 규모 검증이 통과했다.
- 자동 완주를 새 구조에서 재개했다. 재개 직후 상태는 110장면 중 3장면 승인, 현재 V1-E01-S04, 실행 재시도 1회차다.

## 2026-06-22 장면 상호작용 계약 부재

- 자동 완주는 12장면 승인 뒤 V1-E03-S01에서 중단됐다. 이 장면은 센티넬을 피해 증기 파이프 사이에 숨는 은신 장면인데 objective 키워드에 솔로 행동 용어가 없어 대화 필수 장면으로 잘못 분류됐다.
- 현재 하네스는 장면 계약에 등장인물 구성이나 상호작용 유형이 없어서 objective 문자열의 일부 단어로 대화 필요 여부를 추측한다.
- 은신 장면에 대화를 억지로 추가하면 장면의 전술과 개연성이 손상되므로 단순 예외 단어 추가로 해결하지 않는다.
- 구조 생성 단계에서 각 장면의 상호작용 유형과 대화 요구 수준을 Forge가 명시하고, 산문 생성기와 critic은 그 계약만 사용하도록 변경한다.
- 첫 재생성은 구조 후보 JSON을 3회 모두 끝까지 반환하지 못해 실패했다. 산문 길이 확보를 위해 설정한 generator 공통 출력 한도 8,192토큰이 5권 골격 구조 응답까지 잘라낸 것이 원인이므로 32,768토큰으로 확장한다.
- 두 번째 재생성은 Forge가 C1-C21 정본 ID와 임의 ID를 `owns.changes`에 넣어 소유권 검증에서 실패했다. owns는 series.elements 전용이므로 프롬프트에서 canon ID 사용을 명시적으로 금지하고, 정규화 단계에서 선언되지 않은 ID를 제거하며 선언된 요소는 실제 kind에 맞는 owns 배열로 이동한다.
- 세 번째 재생성은 골격 후보를 통과해 권별 확장에 진입했으나 Forge가 owns 배열에 문자열 대신 객체를 넣어 정규화기가 예외를 냈다. 정규화는 문자열 ID만 처리하고 잘못된 타입은 제거해 스키마 검증 또는 재시도가 정상적으로 작동하게 한다.
- 권별 장편 확장은 5권 모두 생성됐지만 독립 정본 critic이 C19의 최종 보스 결정 순서 위반을 발견했다. 기존 오케스트레이터는 critic 거부를 구조 generator에 돌려주지 않고 즉시 종료하므로, 최대 3회까지 critic 오류를 추가 지시로 전달해 전체 구조를 다시 생성하는 피드백 루프를 추가한다.
- 첫 피드백 재시도는 확장 캐시 키가 원본 story 해시만 사용하고 사용자 지시와 critic 피드백을 포함하지 않아 이전 실패 구조를 그대로 재사용했다. 확장 계약 해시에 정규화된 지시문을 포함해 피드백이 달라지면 5권을 실제로 다시 생성하게 한다.

## 2026-06-23 작업 중지와 다음 세션 인수인계

- 사용자의 중지 요청에 따라 `STOP_AFTER_RUN`을 유지한다. 실행 중인 `complete_series.py` 프로세스는 없다.
- 현재 작품은 `심연의 잔향: 침몰하는 도시의 기록자`이며 현재 정본은 장편 확장 전 5권 골격 24장면이다.
- 모든 현재 장면은 `interaction_mode`와 `dialogue_policy`를 가진다. 산문은 현재 정본에 없으며 이전 산문은 `runs/prose-backups/20260622T152439.798912Z` 등에 보존한다.
- 마지막 자동 실행은 구조 확장 중 `'list' object has no attribute 'get'` 오류로 2026-06-23 01:41 KST에 실패했다. `runs/complete-series/status.json`에는 스택 위치 없이 오류 문자열만 남아 있어 다음 세션은 먼저 실제 스택 트레이스를 확보해야 한다.
- 마지막 전체 검증은 `python -m unittest discover -s tests -v` 101개 통과, `python -m compileall -q -f lib pipeline tests` 통과, `python -m pip check` 통과다.
- 생성된 story, reference, state, prose 삭제 상태는 새 세계관과 구조 재생성 과정의 작업 산출물이며 문서 정리 커밋에 포함하지 않는다.

## 2026-06-23 구조 확장 오류 진단 재개

- `STOP_AFTER_RUN`을 유지한 채 구조 확장만 전경 실행해 `'list' object has no attribute 'get'`의 전체 스택 트레이스를 확보한다.
- 오류 입력이 모델의 잘못된 배열·객체 중첩이라면 런타임 예외를 누출하지 않고 권별 검증 오류로 반환해 기존 재시도 루프가 처리하게 한다.
- 수정 뒤 관련 회귀 테스트, 전체 테스트, 구문 검사, 의존성 검사와 5권 구조·규모 검증을 실행한다.
- 사용자 승인 전에는 산문 자동 완주를 시작하지 않는다.

## 2026-06-23 구조 확장 오류 수정과 정본 승격

- 실제 재현 스택에서 `pipeline/expand_structure.py`의 `owned_element_ids()`가 배열형 `scene.owns`에 `.get()`을 호출해 `AttributeError`를 누출하는 위치를 확인했다.
- 객체가 아닌 `owns`와 문자열이 아닌 소유 ID는 집계에서 건너뛰고, JSON Schema 오류가 기존 권별 재시도 루프로 전달되게 수정했다.
- 회귀 테스트는 배열형 `owns` 응답이 런타임 예외 없이 재생성되고 두 번째 정상 응답으로 5권 후보를 완성하는지 검증한다.
- 독립 critic이 첫 확장 후보의 C19 최종 보스 분기 위반을 거부했다. 오류를 구조 generator에 되돌린 후보는 C1-C21 전체 승인을 받았다.
- 승인 후보는 정본으로 승격했고 상태 원장을 `V5-E04-S06`까지 재구축했다.
- 최종 구조는 5권 106장면이며 권별 목표 분량은 V1 80,000자, V2 82,900자, V3 99,900자, V4 89,200자, V5 84,000자다.

## 2026-06-23 상호작용 계약 표본 검증

- 첫 세 장면은 `required`, `optional`, `none` 정책을 각각 검증하는 표본으로 선택했다.
- `V1-E01-S01`과 `V1-E01-S02`는 generator와 critic 승인을 받아 정본 산문으로 승격됐다.
- `V1-E01-S03`은 모델 출력이 2,600자 하한 바로 아래에 반복적으로 머물러 승인되지 않았다.
- 길이 미달 재작성 지시는 검사 하한보다 800자 높은 목표를 사용하고, 과거 길이 오류 지시는 최신 목표로 교체하도록 강화했다. 다른 critic 품질 피드백은 계속 보존한다.
- 강화 뒤에도 마지막 실제 후보는 2,399자로 미달했다. 산문을 직접 수정하거나 하한을 낮추지 않았으며 자동 완주는 재개하지 않았다.

## 2026-06-23 S03 실패와 실행 상태 수정 계획

- S03은 정신적 거부 반응을 견디는 내면 목표만 있어 반복 묘사를 금지한 품질 계약과 4,000자 목표를 동시에 만족시키기 어렵다.
- 장면 계약에 기록 장치 과부하, 물리적 차단 시도, 파편 회수 실패, 동기화를 계속할지 선택하는 외부 진행 비트를 추가한다.
- 같은 산문이 반복 반환되면 새 후보로 취급하지 않고 지속 중복 오류로 조기 중단해 바깥 구조·계약 복구 경로로 넘긴다.
- `httpx.ReadTimeout`과 동등한 timeout·연결 오류는 5xx와 같은 transient 재시도 대상으로 처리한다.
- 수동 구조 승격과 표본 생성 뒤 남은 과거 실패 상태를 현재 106장면·2승인·S03 대기 상태로 갱신한다.

## 2026-06-23 S03 계약과 재시도 복구 완료

- S03 objective에 장치 과열, 기억 역류, 증기 배출핀 조작, 두 차례 차단 실패, 파편 제거와 강제 동기화 사이의 선택을 명시했다.
- 단독 장면 S03의 목표 분량은 3,600자로 조정하고 대화·지형 복구 장면 S04는 4,400자로 조정해 V1 총 80,000자를 유지했다.
- 두 구조 후보 모두 구조·규모 검증과 독립 정본 critic을 통과한 뒤에만 승격했다.
- 새 계약의 S03 산문은 generator와 critic 승인을 받아 정본으로 승격됐다. 현재 승인 산문은 S01-S03 세 장면이다.
- 동일 계약에서 같은 산문 후보가 3회 이상 누적되면 모델을 새로 호출하지 않고 계약 수정 필요 오류를 반환한다.
- `httpx`와 `httpcore`의 timeout·연결 예외를 transient 오류로 분류해 기존 지수 백오프 경로로 재시도한다.
- 대시보드는 최신 완료 상태가 과거 실패 작업보다 새로우면 작업 상태를 `stopped`로 복구하고, 중단 상태에서도 산문 단계 진행률을 표시한다.
- 상태 파일은 106장면 중 3장면 승인, 다음 장면 `V1-E01-S04`, `STOP_AFTER_RUN` 유지로 동기화했다.
- 전체 106개 테스트, 구문 검사, 의존성 검사, 구조·규모 검증과 표본 3장면 해시 검증이 통과했다.

## 2026-06-23 가변 권수와 1권 단위 완성 계획

- Forge가 시놉시스의 사건 밀도, 인물 성장 단계, 복선 회수량을 근거로 적정 권수를 제안한다.
- 추천 권수가 3권 이상이면 사용자 확인 없이 자동 승인한다. 1권 또는 2권 추천만 대시보드 승인 대상으로 둔다.
- 사용자가 권수를 직접 지정하면 별도 승인 없이 해당 권수에 맞춰 시놉시스와 권별 전개를 다시 설계한다.
- 승인된 권수는 이후 구조 생성, 확장, 검증, 상태 원장, EPUB 생성의 유일한 정본 계약이다.
- 테스트 운영에서는 전체 구조를 확정하되 산문은 한 번에 한 권만 완성하고 해당 권 EPUB 생성 뒤 `volume_complete` 상태로 중단한다.
- 재개 명령은 승인된 산문을 재사용하고 다음 미완성 권부터 이어간다.

## 2026-06-23 가변 권수와 1권 단위 완성 구현

- 시놉시스 후보 계약을 추천 권수, 권별 전개, 권수 근거로 변경했다.
- critic 선택 후보가 3권 이상이면 자동 승인하고, 1~2권이면 현재 작품을 백업하거나 교체하기 전에 `volume_approval` 상태로 멈춘다.
- 사용자가 권수를 지정하면 선택 시놉시스의 로그라인과 권별 전개를 해당 권수에 맞게 다시 생성한다.
- `selected-synopsis.json`의 `approved_volume_count`를 구조 생성과 세계관 생성의 권수 계약으로 사용한다.
- 구조 스키마와 ID 검증은 연속된 V1-V99를 허용하며 실제 승인 권수와 정확히 일치해야 한다.
- 오케스트레이터는 다음 미완성 권만 생성·검증·EPUB 내보내기하고, 다음 권이 남으면 `volume_complete`로 종료한다.
- 대시보드는 추천 권수와 근거를 표시하고 직접 권수 지정, 1~2권 승인, 다음 권 재개를 지원한다.
- 기존 `STOP_AFTER_RUN`은 사용자가 대시보드에서 새 실행 또는 재개를 누른 시점에만 제거한다.
- 이전 `five_volume_arc` 후보 파일도 대시보드에서 5권 추천과 권별 전개로 계속 표시한다.
- `python -m unittest discover -s tests -v` 115개, 구문 검사, 의존성 검사, 현재 정본 구조·규모 검증이 통과했다.
- 로컬 대시보드를 실제 브라우저에서 열어 5권 추천·확정 표시, 권별 진행 표시, 자바스크립트 오류 없음까지 확인했다.

## 2026-06-23 대시보드 중단 상태 표시 수정

- 실제 API와 생성 프로세스는 `stopped`였지만 진행 패널의 `V1 진행`, `실행 시간` 문구가 실행 중으로 오해될 수 있었다.
- 중단 상태에서는 재개 예정 권을 표시하고, 종료된 작업 시간은 마지막 실행 시간으로 구분한다.
- 대시보드 테스트 14개와 구문 검사가 통과했고, Tailscale 주소의 실제 화면에서 `중단됨 · 재개 시 V1`, `마지막 실행 시간`을 확인했다.

## 2026-06-23 active 작품과 새 시놉시스 충돌 수정 계획

- 대시보드는 `active_world.status == active`이면 새 후보 생성과 후보 선택 버튼을 모두 막고 있었다.
- 오케스트레이터도 `active`가 있으면 무조건 기존 작품을 재개해 새 후보 생성 명령과 재개 명령을 구분하지 못했다.
- 새 시놉시스 명령에는 현재 작품 교체 의도를 명시하고, 재개 명령은 기존 동작을 유지한다.
- 1~2권 승인 대기까지는 현재 작품을 건드리지 않고, 새 후보가 승인된 뒤에만 현재 작품을 백업하고 교체한다.
- 대시보드의 새 후보 생성과 후보 시작 명령은 `--replace-active`를 사용하고, 기존 작품 재개 명령에는 이 옵션을 넣지 않는다.
- 실제 Tailscale 대시보드에서 현재 작품이 `active`인 상태에서도 새 후보 생성, 후보 시작, 기존 작품 재개 버튼이 모두 활성화됨을 확인했다.
- 전체 테스트 116개, 구문 검사와 의존성 검사가 통과했다.

## 2026-06-23 시놉시스 후보 반복 생성 분리

- 후보 생성 버튼이 `complete_series.py --game-scenario`를 실행해 critic 추천작으로 바로 작품 제작까지 시작하던 것이 재생성을 막은 원인이었다.
- 후보 생성은 `generate_synopses.py --candidates-only`로 후보 5개와 critic 평가만 교체한다.
- 후보 생성 단계에서는 현재 작품, `active.json`, `STOP_AFTER_RUN`을 변경하지 않는다.
- 사용자가 후보를 선택하고 시작 버튼을 누른 뒤에만 권수를 확정하고 현재 작품을 백업·교체한다.
- 후보 세트를 연속 두 번 생성해 두 번째 후보 파일이 정상 게시되는 회귀 테스트를 추가했다.
- 이전 화면에서 잘못 시작된 전체 제작 PID는 아직 `synopsis_selection` 단계여서 종료했고, 현재 작품과 3개 승인 산문 및 `STOP_AFTER_RUN` 중단 상태를 복원했다.
- 전체 테스트 118개, 구문 검사와 의존성 검사가 통과했다.
- 실제 Tailscale 대시보드에서 새 후보 다시 만들기, 후보 선택 시작, 기존 작품 재개 버튼이 모두 활성화되고 현재 상태는 `현재 장면 뒤 중단됨`으로 표시되는 것을 확인했다.

## 2026-06-23 후보 재생성 중 기존 카드 표시 수정

- 새 후보 작업 PID는 정상 실행 중이지만 원자적 게시가 끝날 때까지 이전 후보 파일을 유지하므로 화면 아래 카드도 이전 5개를 그대로 보여줬다.
- 후보 생성 중에는 이전 카드를 숨기고 새 후보와 critic 평가가 진행 중임을 명시한다.
- 실제 Tailscale 화면에서 생성 중 카드 수가 0이고 `완료되면 이 영역이 새 후보로 교체됩니다` 안내가 표시되는 것을 확인했다.
- 실행 중이던 후보 생성은 정상 완료됐고 후보 파일의 원자적 게시 뒤 대시보드에 새 카드 5개가 표시됐다.
- 새 첫 후보는 `심연의 설계자 (The Abyss Architect)`이며, critic 추천은 S3다.
- 관련 테스트 25개와 구문 검사가 통과했고 실제 화면의 카드 수 5개와 재생성 버튼 활성화를 확인했다.

## 2026-06-23 후보 생성 취소와 버튼 복구

- 사용자가 다시 후보 생성을 시작한 PID는 Google API 연결을 유지하며 정상 실행 중이었지만, 완료 전까지 모든 버튼이 비활성이라 새로고침해도 고장처럼 보였다.
- 후보 생성 버튼은 작업 중 `후보 생성 취소`로 전환하고, 취소하면 기존 후보와 현재 작품을 유지한 채 즉시 다시 생성 가능한 상태로 복구한다.
- 완료된 외부 후보 생성 작업은 서버 재기동 뒤 파일 갱신 시각으로 `complete` 판정됐고 세 제어 버튼이 모두 활성화됐다.
- 실제 화면에 새 후보 5개가 표시됐으며 첫 후보는 `잔상 추적자: 기억의 미궁`이다.
- 전체 테스트 119개, 구문 검사와 의존성 검사가 통과했다.
