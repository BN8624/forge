# Forge 구축 체크리스트

- [x] 독립 프로젝트 경계와 5권 목표를 문서화한다.
- [x] Atelier에서 공통 실행 인프라와 최소 참고자료만 복사한다.
- [x] 시리즈, 권, 사건, 장면, 상태 원장 스키마를 정의한다.
- [x] 소유권과 계층 참조를 검사하는 구조 검증기를 구현한다.
- [x] 정상 및 실패 픽스처로 검증기를 테스트한다.
- [x] 프로젝트를 `C:\Users\USER\forge` 독립 경로로 분리한다.
- [x] 독립 Git 저장소와 GitHub 공개 저장소를 연결한다.
- [x] JSON Schema를 구조 검증기에 연결하고 계약 위반 테스트를 추가한다.
- [x] 구조 문서 후보 생성기를 구현한다.
  - [x] 모델 응답을 시리즈, 권, 사건, 장면 문서로 분해한다.
  - [x] 생성된 후보 전체를 구조 검증기로 검사한다.
  - [x] 응답 파싱 또는 구조 검증 실패 시 기존 후보 출력을 보존한다.
  - [x] 검증된 후보 디렉터리만 출력 경로에 원자적으로 게시한다.
  - [x] 구조 검증 오류를 모델에 피드백해 제한 횟수 재생성한다.
  - [x] 장면 순서에서 파생 가능한 상태 연속성 필드를 결정적으로 정규화한다.
- [x] 독립 검증과 정본 승격의 원자적 흐름을 구현한다.
  - [x] 후보 `story`를 현재 정본과 분리된 임시 스냅샷에서 검증한다.
  - [x] 검증 실패 또는 교체 오류 시 기존 `story`가 보존되는지 테스트한다.
  - [x] 중단된 교체를 다음 실행에서 복구한다.
  - [x] 검증을 통과한 후보만 `story` 정본으로 승격한다.
- [x] 상태 원장 갱신과 재실행 검증을 구현한다.
  - [x] 검증된 구조에서 최종 장면, 상태, 적용 요소를 결정적으로 파생한다.
  - [x] 원장을 원자적으로 저장하고 실패 시 기존 파일을 보존한다.
  - [x] 같은 구조로 재실행한 원장 파일이 바이트 단위로 같은지 검사한다.
  - [x] 구조 검증기가 적용 요소 목록의 불일치도 거부한다.
- [x] 5권 신규 구조 후보 생성을 시작한다.
  - [x] 기존 C1-C21 정본과 압축 원고를 생성 입력에 포함한다.
  - [x] Forge가 기존 10권 권 배치를 승계하지 않고 5권 구조를 생성한다.
- [x] C1-C21의 장면별 추적성과 의미 준수를 독립 검증한다.
  - [x] critic이 각 정본 항목의 판정, 근거 장면, 이유를 구조화해 출력한다.
  - [x] C1-C21 전체 통과와 후보 story 해시 일치를 결정적으로 검사한다.
  - [x] 승인된 독립 검토가 없거나 오래된 후보의 정본 승격을 차단한다.
- [x] 장면별 산문 생성과 독립 검증 파이프라인을 구현한다.
  - [x] 정본 장면 순서와 이전 정본 산문을 생성 입력으로 사용한다.
  - [x] critic이 장면 계약, 상태, 정본 설정, 산문 완결성을 판정한다.
  - [x] 실패 판정을 생성 모델에 피드백해 해당 장면만 재생성한다.
  - [x] 승인된 산문과 검토를 하나의 원자적 장면 단위로 승격한다.
- [x] Forge가 각 권을 최소 8만 자 규모의 사건·장면 구조로 확장한다.
  - [x] 권별로 최소 20개 장면과 목표 분량 8만 자를 생성한다.
  - [x] 기존 권 목표, C1-C21, 이야기 요소 소유권 집합을 유지한다.
  - [x] 5권을 권별 생성한 뒤 하나의 후보로 조립하고 전체 검증한다.
- [x] Forge가 첫 장면 산문을 생성하고 강화된 critic 검증을 통과시킨다.
- [x] V1의 29개 장면 산문을 순차 생성·검증한다.
- [x] 남은 97개 장면 산문을 순차 생성·검증한다.
  - [x] V2 첫 10개 장면을 Forge로 생성·검증한다.
  - [x] V2 다음 10개 장면을 Forge로 생성·검증한다.
  - [x] V2 남은 3개 장면을 Forge로 생성·검증한다.
  - [x] V3 전체 장면을 Forge로 생성·검증한다.
  - [x] V4 전체 장면을 Forge로 생성·검증한다.
  - [x] V5 전체 장면을 Forge로 생성·검증한다.
- [x] 5권 전체 산문과 EPUB을 최종 검증한다.
- [x] 기존 세계관 입력부터 5권 완성까지 한 명령으로 자동화한다.
  - [x] 유효한 정본·후보·승인 산문을 재사용해 중단 지점부터 재개한다.
  - [x] 구조 생성·정본 검증·승격·규모 확장·상태 재구성을 자동 연결한다.
  - [x] 남은 산문 생성·critic 승인·전권 EPUB·최종 검증을 자동 연결한다.
  - [x] 구조 교체 시 기존 산문을 안전하게 작업 백업한다.
  - [x] 자동화 재실행과 실패 재개를 테스트한다.
- [x] 신규 세계관 생성부터 5권 완성까지 한 명령으로 자동화한다.
  - [x] 기존 세계관 고유어를 배제한 신규 원천 세계관을 생성·검증한다.
  - [x] 신규 세계관을 이후 구조·critic·산문 단계의 우선 정본으로 연결한다.
  - [x] 기존 완성본을 백업하고 중단된 신규 세계관 실행을 재개한다.
  - [x] 장르·소재 선택 지시 없이도 전 과정을 시작할 수 있게 한다.
- [x] iPhone에서 V1을 읽을 수 있는 Tailscale 전용 뷰어를 제공한다.
  - [x] V1 정본 순서대로 산문을 읽기 전용 HTML로 렌더링한다.
  - [x] 모바일 화면과 기본 탐색을 검증한다.
  - [x] 로컬 서버를 Tailscale IP에 바인딩해 tailnet 안에만 공개한다.
- [x] V1을 iPhone 도서 앱용 EPUB으로 제공한다.
  - [x] 정본 순서에서 표준 EPUB 파일을 생성한다.
  - [x] Tailscale 뷰어에 EPUB 다운로드를 연결한다.
  - [x] EPUB 필수 파일과 장면 순서를 검증한다.
- [x] 신규 세계관의 실제 5권 완주 결과를 정본으로 보존한다.
  - [x] 신규 세계관 원천 문서를 `reference/current`에 보존한다.
  - [x] 108개 장면 산문과 critic 승인을 검증한다.
  - [x] V1-V5 EPUB을 생성하고 전권 서재를 새 작품으로 전환한다.
  - [x] iPhone 390×844 화면에서 제목, 전권 링크, 가로 넘침을 검증한다.
- [x] 게임 시나리오용 소설 기획 선택을 Forge에 맡긴다.
  - [x] 서로 다른 게임화 방향의 시놉시스 후보 5개를 생성한다.
  - [x] 독립 critic이 후보 전체를 평가하고 하나를 선택한다.
  - [x] 선택 근거와 보완 지시를 신규 세계관 생성 입력으로 연결한다.
  - [x] 후보·평가·선택 결과를 현재 세계관 정본과 함께 보존한다.
  - [x] 한 명령으로 선택 이후 5권 자동 완주까지 이어지게 한다.
- [x] iPhone에서 후보를 확인하고 생성을 시작하는 대시보드를 제공한다.
  - [x] 후보 생성과 작업 상태 조회를 Tailscale 서재에 연결한다.
  - [x] 후보 5개, critic 추천, 강점·위험, 5권 전개를 모바일 카드로 표시한다.
  - [x] critic 추천 또는 사용자 선택 후보로 백그라운드 자동 완주를 시작한다.
  - [x] 동시 작업과 위조 POST 요청을 차단한다.
  - [x] 새 작품 완주 뒤 서재가 서버 재시작 없이 현재 정본을 표시한다.
  - [x] 실제 Tailscale 서버와 iPhone 크기에서 전체 화면을 검증한다.
- [x] 대시보드에 자동 완주의 정확한 현재 상태를 표시한다.
  - [x] 대상 작품과 현재 파이프라인 단계를 한국어로 표시한다.
  - [x] 전체 장면, 승인 장면, 현재 장면 번호와 ID를 표시한다.
  - [x] 현재 권, critic 재시도 횟수, 마지막 갱신 시각을 표시한다.
  - [x] iPhone 화면에서 진행 정보의 가독성을 검증한다.
- [x] 산문의 대화 비중과 사건 진행 속도를 강화한다.
  - [x] 정적인 묘사·설명 목표 대신 행동·충돌·선택 중심 장면 계약을 요구한다.
  - [x] generator에 대화, 행동 비트, 장면 내 반전과 결정 기준을 추가한다.
  - [x] critic이 반복 묘사, 정보 전달용 독백, 상태 변화 없는 장면을 거부한다.
  - [x] 대화가 필요한 장면의 최소 대화량을 하네스에서 선검사한다.
  - [x] 기존 승인 산문을 새 품질 계약에서 무효화하고 안전하게 재생성한다.
- [x] 산문 생성의 과도한 동일 장면 재시도를 제거한다.
  - [x] V1-E02-S03의 실제 실패 조건과 장면 계약 충돌을 확인한다.
  - [x] 실패 사유가 다음 생성 시도에 구체적으로 반영되는지 검증한다.
  - [x] 무제한 반복 대신 구조 결함을 드러내고 복구하는 경로를 추가한다.
  - [x] 행동 중심 구조로 재생성한 표본을 합리적인 시도 횟수 안에 승인시킨다.
  - [x] 전체 자동 생성을 새 구조와 하네스로 재개한다.
- [ ] 장면의 대화 필요 여부를 키워드로 추측하는 구조를 제거한다.
  - [x] 장면 계약에 상호작용 유형과 대화 요구 수준을 명시한다.
  - [x] 구조 generator와 확장기가 해당 값을 장면 내용에 맞게 결정한다.
  - [x] 산문 선검사와 critic이 명시된 계약만 사용한다.
  - [x] 현재 구조를 새 계약으로 재생성하고 표본을 검증한다.
  - [ ] 자동 완주를 재개한다.
- [x] 구조 확장 런타임의 `'list' object has no attribute 'get'` 오류를 진단한다.
  - [x] 실제 스택 트레이스를 보존해 오류 발생 위치를 확인한다.
  - [x] 잘못된 모델 응답은 예외가 아니라 검증 오류와 재시도로 처리한다.
  - [x] 5권 장편 구조가 독립 정본 critic을 통과하는지 확인한다.
- [x] S03 표본 실패와 자동 실행 상태 불일치를 수정한다.
  - [x] S03 계약에 외부 행동, 방해, 선택과 결과를 명시한다.
  - [x] 동일 산문 후보 반복을 감지해 불필요한 호출을 중단한다.
  - [x] 네트워크 읽기 시간 초과를 transient 오류로 재시도한다.
  - [x] 완료 상태와 대시보드 상태를 현재 정본·산문 수량과 맞춘다.
  - [x] 수정된 구조의 독립 critic과 전체 테스트를 통과시킨다.
- [x] Forge가 작품별 적정 권수를 결정하고 1권 단위로 완성한다.
  - [x] 시놉시스에 추천 권수와 권별 전개를 생성한다.
  - [x] 3권 이상 추천은 자동 승인하고 1~2권만 사용자 승인을 기다린다.
  - [x] 사용자 지정 권수는 즉시 확정하고 시놉시스를 해당 권수로 재설계한다.
  - [x] 구조·스키마·검증·확장·상태 원장의 5권 고정을 제거한다.
  - [x] 첫 실행은 1권 산문과 EPUB 완성 뒤 자동 중단한다.
  - [x] 재개 시 다음 미완성 권부터 이어서 생성한다.
  - [x] 대시보드에서 추천·확정 권수와 1권 단위 진행을 표시한다.
- 마지막 전체 검증은 `python -m unittest discover -s tests -v` 115개 통과, `python -m compileall -q -f lib pipeline tests` 통과, `python -m pip check` 통과다.
